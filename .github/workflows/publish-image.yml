name: Publish image

on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Build image
        run: |
          sha="$(git rev-parse --short=7 HEAD)"
          docker build --platform linux/amd64 \
            --tag "ghcr.io/4mdosx/navi:${sha}" \
            --tag ghcr.io/4mdosx/navi:main \
            .
          echo "IMAGE_SHA=${sha}" >> "$GITHUB_ENV"

      - name: Push image
        run: |
          docker push "ghcr.io/4mdosx/navi:${IMAGE_SHA}"
          docker push ghcr.io/4mdosx/navi:main
